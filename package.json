{
  "name": "@squarespace/fonts-loaded",
  "version": "1.0.0",
  "author": "Squarespace, Inc.",
  "description": "Determine if fonts are loaded for particular nodes",
  "license": "SEE LICENSE IN LICENSE.txt",
  "scripts": {
    "lint": "eslint src",
    "build": "npm run lint && babel src --out-dir dist",
    "prepublish": "npm run build",
    "watch": "npm run lint && babel src --watch --out-dir dist"
  },
  "devDependencies": {
    "eslint-config-squarespace": "^1.0.0",
    "babel-cli": "^6.7.5",
    "babel-eslint": "^6.1.2",
    "babel-plugin-transform-runtime": "^6.9.0",
    "babel-preset-es2015": "^6.6.0",
    "eslint": "^3.0.1"
  },
  "dependencies": {
    "babel-runtime": "^6.9.0"
  },
  "module": "src/index.js",
  "main": "dist/index.js"
}
